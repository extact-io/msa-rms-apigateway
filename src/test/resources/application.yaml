config_ordinal: 201
#----------------------------------
# -apigateway.jar Test Configurations
#----------------------------------
env.rms:
  service.url:
    item: http://localhost:7001
    reservation: http://localhost:7001
    user: http://localhost:7001

rms:
  debug:
    configdump:
      filter-enable: true
      filters:
        #- filter: security
        - filter: server
